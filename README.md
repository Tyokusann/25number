# NUMBER 25

1から25までの数字を順番にタップする時間計測ゲームです。スマートフォン・タブレットのSafariを含むモダンブラウザで動作します。

## 起動

`index.html` をブラウザで開くか、このフォルダーを任意の静的Webサーバーで配信してください。

例:

```powershell
python -m http.server 8000
```

同じWi-Fi内のスマートフォンからアクセスする場合は、PCのローカルIPアドレスを使って `http://<PCのIP>:8000` を開きます。
